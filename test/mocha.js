
const assert = require("assert");

// for basic setup; will remove once build pipeline is solid
describe("Basic test", () => {
  it("Should never fail under any circumstances", () => {
    assert.equal(true, true);
  });
});
